# Calibration profiles

`lab_unit/` contains measurements from one physical LeKiwi/SO-101. They are
published for reproducibility, not as universal defaults. In particular, do not
reuse its motor ranges, joint offsets, hand-eye transform, depth affine map, or
floor baseline on another robot without recalibration.
